name: tests
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    container: 
      image: python:3.10
    steps:
      - name: download code
        uses: actions/checkout@v3
      - name: install package and dependencies
        run: make
      - name: run tests
        run: make test
