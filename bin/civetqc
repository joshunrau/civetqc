#!/usr/bin/env python

import argparse
import pickle
import sys
import os
import civetqc.config as cfg


def load_saved_model(filepath: str):
    with open(filepath, 'rb') as f:
        return pickle.load(f)

def main():
    print(cfg.ModulePaths.root)
    print(cfg.StudyPaths.root, cfg.StudyPaths.TOPSY)

    #parser = argparse.ArgumentParser(prog="civetqc")
    #parser.add_argument("path_csv", help="path to csv file outputted by CIVET")
    # args = parser.parse_args(sys.argv[1:])
    #args = parser.parse_args([SIMULATED_CIVET])

    #civet_data = CIVETData(args.path_csv)
    #print(civet_data.df)



if __name__ == "__main__":
    main()
